## hero
https://www.npmjs.com/package/@gsap/react
1. .hero-title span  각각 React + @gsap/react의 useGSAP 훅을 사용해서 SplitText.create()로  텍스트를 글자 단위로 분해하고, 각 글자가 아래에서 위로 순차적으로 페이드인하는 컴포넌트를 만들어줘. stagger 0.03초, ease는 power3.out으로 설정하고, gsap.context()로 클린업을 처리해줘.